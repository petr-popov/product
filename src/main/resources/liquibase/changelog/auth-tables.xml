<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">

    <property name="now" value="now()" dbms="h2"/>

    <changeSet id="create-sequence-user" author="popov">
        <createSequence sequenceName="user_brand" startValue="1000" incrementBy="50"/>
    </changeSet>

    <changeSet id="auth-tables" author="popov">

        <createTable tableName="user">
            <column name="id" type="bigint" autoIncrement="${autoIncrement}">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="login" type="varchar(50)">
                <constraints unique="true" nullable="false"/>
            </column>
            <column name="password_hash" type="varchar(60)"/>
        </createTable>

        <createIndex indexName="idx_user_login"
            tableName="user"
            unique="true">
            <column name="login" type="varchar(50)"/>
        </createIndex>

        <createTable tableName="user_authority">
            <column name="user_id" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="authority_name" type="varchar(50)">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createTable tableName="authority">
            <column name="name" type="varchar(50)">
                <constraints primaryKey="true" nullable="false"/>
            </column>
        </createTable>

        <addPrimaryKey columnNames="user_id, authority_name" tableName="user_authority"/>

        <addForeignKeyConstraint baseColumnNames="authority_name"
                                 baseTableName="user_authority"
                                 constraintName="fk_authority_name"
                                 referencedColumnNames="name"
                                 referencedTableName="authority"/>

        <addForeignKeyConstraint baseColumnNames="user_id"
                                 baseTableName="user_authority"
                                 constraintName="fk_user_id"
                                 referencedColumnNames="id"
                                 referencedTableName="user"/>

        <loadData encoding="UTF-8"
                  file="liquibase/users.csv"
                  separator=";"
                  tableName="user">
        </loadData>

        <loadData encoding="UTF-8"
                  file="liquibase/authorities.csv"
                  separator=";"
                  tableName="authority"/>

        <loadData encoding="UTF-8"
                  file="liquibase/users_authorities.csv"
                  separator=";"
                  tableName="user_authority"/>


    </changeSet>

</databaseChangeLog>
